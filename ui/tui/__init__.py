"""Target Textual TUI package.

The full-screen interface is built on the application SessionController. The
projection module turns the controller's history snapshot and ordered updates
into a single, rebuildable message tree; the view (M4) renders that tree.
"""

from __future__ import annotations

__all__: list[str] = []
